# The same project the app points at. Both keys are publishable by design and
# ship inside any client build; the service-role key is not used here or
# anywhere else in this repository.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
